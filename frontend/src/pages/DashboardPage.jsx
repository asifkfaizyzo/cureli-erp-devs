// src/pages/BillingPage.jsx
import AppLayout from "../components/layout/AppLayout";

// Billing page content (no props now — uses global store)
const DashboardPage = () => {
  
  return (
    
    <AppLayout/>
  );
};

export default DashboardPage;
